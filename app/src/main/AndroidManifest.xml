<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lalit.rxjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RxJava">
        <activity android:name=".ui.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MergeAndConcatOperatorActivity" />
        <activity android:name=".ui.FlatmapOperatorActivity" />
        <activity android:name=".ui.ZipOperatorActivity" />
        <activity android:name=".ui.MapOperatorActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>